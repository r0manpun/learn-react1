const College = ({ name, address, affilation }) => {
  return (
    <div>
      <p>Name: {name}</p>
      <p>Address: {address}</p>
      <p>Affilation: {affilation}</p>
    </div>
  );
};

export default College;
